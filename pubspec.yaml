name: kelompok2
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.7.0
  flutter_svg: ^2.0.9
  ionicons: ^0.2.2
  provider: ^6.1.2
  flutter_colorpicker: ^1.0.0
  webview_flutter: ^4.2.0
  http: ^1.5.0
  shared_preferences: ^2.2.3
  image_picker: ^1.0.7
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  cloudinary_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Tambahkan semua folder aset di sini
  assets:
    - assets/images/
    - assets/icons/
